## Packages
date-fns | Date formatting and manipulation for log entries and stats
recharts | Visualization for calorie and protein trends in analytics
framer-motion | Smooth animations for page transitions and micro-interactions
lucide-react | Icons for the UI (already in base, but noting usage)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
